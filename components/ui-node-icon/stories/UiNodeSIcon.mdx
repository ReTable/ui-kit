import { ArgsTable, Canvas, Meta, Source } from '@storybook/blocks';

import * as Stories from './UiNodeSIcon.stories';

<Meta of={Stories} />

# Ui*SIcon

We provide icons as React components.

## Import

You can import a single icon the following way:

<Source code={`
import { UiInnerJoinSIcon } from '@tabula/ui-node-icon';
`} language="ts" />

Or you can import all icons another way:

<Source code={`
import { UiNodeSIcons } from '@tabula/ui-node-icon';

const ComponentWithIcon = () => <UiNodeSIcons.UiInnerJoinSIcon />;
`} language="tsx"/>

## Default

Data types icons has `16x16` size by default.

<Canvas layout="centered" of={Stories.Default} />

## Styling

You can style icons with CSS.

### Color

All data types icons inherit `currentColor` property.

<Canvas layout="centered" of={Stories.Color} />

### Branded icons

Some icons have predefined colors which can't be changed.

<Canvas layout="centered" of={Stories.Branded} />

### Size with class

You can provide `className` property.

<Canvas layout="centered" of={Stories.SizeWithClassName} />

### Size with style

You can provide `style` property.

<Canvas layout="centered" of={Stories.SizeWithStyle} />
