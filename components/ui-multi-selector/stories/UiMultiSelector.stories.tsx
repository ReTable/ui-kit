import { FC } from 'react';

import { UiMultiSelector } from '~';

export default {
  component: UiMultiSelector,
  title: 'ui-multi-selector',
};

export const Default: FC = () => <UiMultiSelector />;
