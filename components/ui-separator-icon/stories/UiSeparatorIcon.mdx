import { Canvas, Meta, Source } from '@storybook/blocks';

import * as Stories from './UiSeparatorIcon.stories';

<Meta of={Stories} />

# Ui*Icon

We provide icons as React components.

## Import

<br />

<Source code={`
import { UiCommaIcon } from '@tabula/ui-separator-icon';
`} language="ts"/>

<br />

## Default

Data types icons has `16x16` size by default.

<Canvas layout="centered" of={Stories.Default} />

## Styling

You can style icons with CSS.

### Color

All data types icons inherit `currentColor` property.

<Canvas layout="centered" of={Stories.Color} />

### Size with class

You can provide `className` property.

<Canvas layout="centered" of={Stories.SizeWithClassName} />

### Size with style

You can provide `style` property.

<Canvas layout="centered" of={Stories.SizeWithStyle} />

# Image URL

Also, we provide icons as static images.

## Import

<br />

<Source code={`
import { uiCommaIconUrl } from "@tabula/ui-separator-type-icon";
`} language="ts"/>

<br />

## CSS / Sass

If you use a Vite bundler, you can use icons in your styles like this:

<Source code={`
.awesome-component {
  background-image: url('@tabula/ui-separator-icon/comma.svg');
}
`} language="css"/>
