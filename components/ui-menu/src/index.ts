export { UiMenu } from './UiMenu';
export type { UiMenuProps } from './UiMenu';

export { sizes as uiMenuSizes } from './const';

export type {
  ConfigItem as UiMenuItemConfig,
  Divider as UiMenuDivider,
  IconComponentType as UiMenuIconComponentType,
  Item as UiMenuItem,
  ItemWithSubMenu as UiMenuItemWithSubMenu,
  Size as UiMenuSize,
  Title as UiMenuTitle,
  Variant as UiMenuVariant,
} from './types';
