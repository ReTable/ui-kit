export { isOpenLine, isCloseLine, isValueLine } from './predicates';
export { toLines } from './toLines';
export { LineKind } from './types';

export type { Line, PrimitiveLineKind, ValueType } from './types';
