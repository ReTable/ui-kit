import { Canvas, Meta } from '@storybook/blocks';

import { UiAnalytics } from '~';

import { Table } from './Table';
import { UseUiTrackId } from './UseUiTrackId';

<Meta title="Docs/useUiTrackId" />

# useUiTrackId

This hook allows to fetch track id from context.

## Type

`function useUiTrackId(id?: string): string | null`

## Usage

<Table
  columns={['Track Id', 'Id', 'Result']}
  data={[
    [null, null, null],
    [null, '', null],
    [null, 'child', null],
    ['parent', null, 'parent'],
    ['parent', '', 'parent'],
    ['parent', 'child', 'parent--child'],
  ]}
/>

## Demo

<Canvas>
  <UiAnalytics trackId="parent">
    <UseUiTrackId />
    <UseUiTrackId id="child" />
  </UiAnalytics>
</Canvas>
